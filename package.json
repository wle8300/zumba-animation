{
  "name": "zumba-animation",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://wle8300.github.io/zumba-animation",
  "devDependencies": {
    "react-scripts": "1.1.0"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
